<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Squid Game: Legion [RP] Staff Portal</title>
    <style>
        /* === GLOBAL STYLES === */
        :root {
            --sg-red: #e84118;
            --sg-pink: #fd79a8;
            --sg-blue: #0984e3;
            --sg-green: #00b894;
            --terminal-text: #00ff41;
            --bg-dark: #0d0d0d;
            --bg-darker: #080808;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Courier New', monospace;
        }
        
        body {
            background-color: var(--bg-darker);
            color: var(--terminal-text);
            overflow-x: hidden;
            height: 100vh;
        }
        
        /* === TERMINAL EFFECTS === */
        .scanlines {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: repeating-linear-gradient(
                0deg,
                rgba(0, 255, 65, 0.03),
                rgba(0, 255, 65, 0.03) 1px,
                transparent 1px,
                transparent 2px
            );
            pointer-events: none;
            z-index: 9999;
        }
        
        .glitch-effect {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" width="100%" height="100%"><filter id="noise"><feTurbulence type="fractalNoise" baseFrequency="0.7" numOctaves="3" stitchTiles="stitch"/></filter><rect width="100%" height="100%" filter="url(%23noise)" opacity="0.05"/></svg>');
            pointer-events: none;
            z-index: -1;
        }
        
        /* === LOGIN SCREEN === */
        #login-screen {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            background: radial-gradient(ellipse at center, var(--bg-dark) 0%, var(--bg-darker) 100%);
            z-index: 1000;
            transition: opacity 0.5s ease;
        }
        
        .login-container {
            width: 400px;
            padding: 40px;
            background: rgba(13, 13, 13, 0.9);
            border: 1px solid var(--sg-red);
            box-shadow: 0 0 30px rgba(232, 65, 24, 0.3);
            text-align: center;
            position: relative;
            overflow: hidden;
        }
        
        .login-container::before {
            content: "";
            position: absolute;
            top: -10px;
            left: -10px;
            right: -10px;
            bottom: -10px;
            border: 2px solid var(--sg-pink);
            animation: borderPulse 4s linear infinite;
            pointer-events: none;
            opacity: 0.7;
        }
        
        @keyframes borderPulse {
            0% { border-color: var(--sg-pink); }
            50% { border-color: var(--sg-blue); }
            100% { border-color: var(--sg-pink); }
        }
        
        .login-logo {
            width: 200px;
            margin-bottom: 30px;
            filter: drop-shadow(0 0 10px var(--sg-red));
        }
        
        .login-title {
            font-size: 1.8rem;
            margin-bottom: 30px;
            color: white;
            text-shadow: 0 0 10px var(--sg-red);
            letter-spacing: 3px;
            position: relative;
        }
        
        .login-title::after {
            content: "";
            position: absolute;
            bottom: -10px;
            left: 50%;
            transform: translateX(-50%);
            width: 100px;
            height: 2px;
            background: linear-gradient(90deg, transparent, var(--sg-red), transparent);
        }
        
        .login-input-group {
            margin-bottom: 20px;
            text-align: left;
        }
        
        .login-label {
            display: block;
            margin-bottom: 8px;
            color: var(--sg-pink);
            font-size: 0.9rem;
            letter-spacing: 1px;
        }
        
        .login-input {
            width: 100%;
            padding: 12px 15px;
            background: rgba(0, 0, 0, 0.5);
            border: 1px solid #333;
            color: white;
            font-size: 1rem;
            transition: all 0.3s;
        }
        
        .login-input:focus {
            outline: none;
            border-color: var(--sg-red);
            box-shadow: 0 0 10px rgba(232, 65, 24, 0.5);
        }
        
        .login-btn {
            width: 100%;
            padding: 12px;
            margin-top: 20px;
            background: linear-gradient(135deg, var(--sg-red), #c23616);
            border: none;
            color: white;
            font-size: 1rem;
            font-weight: bold;
            letter-spacing: 1px;
            cursor: pointer;
            transition: all 0.3s;
            position: relative;
            overflow: hidden;
        }
        
        .login-btn:hover {
            background: linear-gradient(135deg, #ff4757, var(--sg-red));
            box-shadow: 0 0 15px rgba(232, 65, 24, 0.7);
        }
        
        .login-btn::after {
            content: "";
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: all 0.5s;
        }
        
        .login-btn:hover::after {
            left: 100%;
        }
        
        .login-error {
            color: var(--sg-red);
            margin-top: 15px;
            font-size: 0.9rem;
            display: none;
        }
        
        /* === DATABASE SCREEN === */
        #database-screen {
            display: none;
            width: 100%;
            min-height: 100vh;
            padding-top: 50px;
            position: relative;
        }
        
        .url-bar {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            background: #111;
            padding: 8px 20px;
            border-bottom: 1px solid #333;
            display: flex;
            align-items: center;
            z-index: 100;
        }
        
        .url-lock {
            color: var(--sg-green);
            margin-right: 8px;
        }
        
        .url-text {
            color: #ddd;
            font-size: 0.9rem;
        }
        
        .url-security {
            margin-left: auto;
            color: var(--sg-green);
            font-size: 0.8rem;
        }
        
        .database-header {
            text-align: center;
            padding: 30px 0;
            position: relative;
        }
        
        .database-title {
            font-size: 2rem;
            letter-spacing: 3px;
            margin-bottom: 10px;
            text-shadow: 0 0 10px var(--sg-red);
            background: linear-gradient(90deg, var(--sg-pink), var(--sg-red));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        
        .database-subtitle {
            color: #aaa;
            font-size: 0.9rem;
            letter-spacing: 1px;
        }
        
        .database-content {
            width: 90%;
            max-width: 1200px;
            margin: 0 auto;
            padding-bottom: 50px;
        }
        
        .database-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 30px;
        }
        
        .database-table th {
            background-color: #111;
            padding: 15px;
            text-align: left;
            border-bottom: 2px solid var(--sg-red);
            color: var(--sg-pink);
            letter-spacing: 1px;
            font-size: 0.9rem;
        }
        
        .database-table td {
            padding: 12px 15px;
            border-bottom: 1px solid #222;
            vertical-align: middle;
        }
        
        .database-table tr:nth-child(even) {
            background-color: rgba(17, 17, 17, 0.5);
        }
        
        .database-table tr:hover {
            background-color: rgba(232, 65, 24, 0.05);
        }
        
        .agent-avatar {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            border: 2px solid var(--sg-blue);
            box-shadow: 0 0 10px rgba(9, 132, 227, 0.3);
            transition: all 0.3s;
        }
        
        .agent-avatar:hover {
            transform: scale(1.1);
            border-color: var(--sg-pink);
            box-shadow: 0 0 15px rgba(253, 121, 168, 0.5);
        }
        
        .rank-mastermind {
            color: var(--sg-red);
            font-weight: bold;
            text-shadow: 0 0 5px rgba(232, 65, 24, 0.5);
        }
        
        .status-active {
            color: var(--sg-green);
            position: relative;
            padding-left: 15px;
        }
        
        .status-active::before {
            content: "";
            position: absolute;
            left: 0;
            top: 50%;
            transform: translateY(-50%);
            width: 8px;
            height: 8px;
            background-color: var(--sg-green);
            border-radius: 50%;
            animation: pulse 2s infinite;
        }
        
        @keyframes pulse {
            0% { opacity: 0.5; box-shadow: 0 0 0 0 rgba(0, 184, 148, 0.7); }
            70% { opacity: 1; box-shadow: 0 0 0 10px rgba(0, 184, 148, 0); }
            100% { opacity: 0.5; box-shadow: 0 0 0 0 rgba(0, 184, 148, 0); }
        }
        
        .profile-link {
            color: var(--sg-blue);
            text-decoration: none;
            font-weight: bold;
            transition: all 0.3s;
            display: inline-flex;
            align-items: center;
        }
        
        .profile-link:hover {
            color: var(--sg-pink);
            text-shadow: 0 0 5px rgba(253, 121, 168, 0.5);
        }
        
        .profile-link::after {
            content: "↗";
            margin-left: 5px;
            font-size: 0.8rem;
        }
        
        .database-footer {
            text-align: center;
            margin-top: 40px;
            padding: 20px;
            border-top: 1px solid #333;
            font-size: 0.8rem;
            color: #666;
        }
        
        .clearance-badge {
            display: inline-block;
            background: linear-gradient(90deg, var(--sg-red), var(--sg-pink));
            color: white;
            padding: 3px 10px;
            border-radius: 3px;
            font-weight: bold;
            font-size: 0.7rem;
            margin-left: 5px;
            letter-spacing: 1px;
        }
        
        /* === ANIMATIONS === */
        @keyframes flicker {
            0%, 19.999%, 22%, 62.999%, 64%, 64.999%, 70%, 100% {
                opacity: 1;
            }
            20%, 21.999%, 63%, 63.999%, 65%, 69.999% {
                opacity: 0.5;
            }
        }
        
        .flicker {
            animation: flicker 10s linear infinite;
        }
    </style>
</head>
<body>
    <div class="scanlines"></div>
    <div class="glitch-effect"></div>
    
    <!-- Login Screen -->
    <div id="login-screen">
        <div class="login-container">
            <img src="SQUID GAME LEGION ROLEPLAY.png" class="login-logo" alt="Squid Game Logo">
            <h2 class="login-title flicker">SECURE ACCESS</h2>
            
            <div class="login-input-group">
                <label class="login-label">SECURITY ID</label>
                <input type="text" class="login-input" id="login-username" placeholder="SG-XXXXXX">
            </div>
            
            <div class="login-input-group">
                <label class="login-label">ACCESS CODE</label>
                <input type="password" class="login-input" id="login-password" placeholder="••••••••">
            </div>
            
            <button class="login-btn" id="login-button">AUTHENTICATE</button>
            <div class="login-error" id="login-error">ACCESS DENIED: INVALID CREDENTIALS</div>
        </div>
    </div>
    
    <!-- Database Screen -->
    <div id="database-screen">
        <div class="url-bar">
            <span class="url-lock">🔒</span>
            <span class="url-text">https://squidgame-legion-rp/staff/database/secure</span>
            <span class="url-security">SECURE CONNECTION</span>
        </div>
        
        <div class="database-header">
            <h1 class="database-title">MANAGERS DATABASE</h1>
            <p class="database-subtitle">CLASSIFIED INFORMATION - MANAGERS CLEARANCE REQUIRED</p>
        </div>
        
        <div class="database-content">
            <table class="database-table">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>AGENT</th>
                        <th>CODENAME</th>
                        <th>RANK</th>
                        <th>STATUS</th>
                        <th>PROFILE</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>M.M1</td>
                        <td><img src="https://tr.rbxcdn.com/30DAY-AvatarHeadshot-A930594FFBED8A2AD1183EE0ACC59715-Png/150/150/AvatarHeadshot/Webp/noFilter"  class="agent-avatar" alt="Agent Avatar"></td>
                        <td>mbfmydtodotd</td>
                        <td class="rank-mastermind">MASTERMIND</td>
                        <td class="status-active">ACTIVE</td>
                        <td><a href="https://www.roblox.com/users/4540893667/profile" target="_blank" class="profile-link">INSPECT</a></td>
                    </tr>
                    <tr>
                        <td>M.M2</td>
                        <td><img src="https://tr.rbxcdn.com/30DAY-AvatarHeadshot-AB9D40118D74CAEA6AC1D62F1ADD0876-Png/150/150/AvatarHeadshot/Webp/noFilter" class="agent-avatar" alt="Agent Avatar"></td>
                        <td>TheGreatGeneral79</td>
                        <td class="rank-mastermind">MASTERMIND</td>
                        <td class="status-active">ACTIVE</td>
                        <td><a href="https://www.roblox.com/users/3188382164/profile" target="_blank" class="profile-link">INSPECT</a></td>
                    </tr>
                    <tr>
                        <td>001</td>
                        <td><img src="https://tr.rbxcdn.com/30DAY-AvatarHeadshot-A930594FFBED8A2AD1183EE0ACC59715-Png/150/150/AvatarHeadshot/Webp/noFilter"  class="agent-avatar" alt="Agent Avatar"></td>
                        <td>Jaydensmarts7</td>
                        <td class="rank-mastermind">ADVISING MANAGER</td>
                        <td class="status-active">ACTIVE</td>
                        <td><a href="https://tr.rbxcdn.com/30DAY-AvatarHeadshot-A5FEA8170C208034816A1A0A4BFF9D04-Png/150/150/AvatarHeadshot/Webp/noFilter" target="_blank" class="profile-link">INSPECT</a></td>
                    </tr>
                </tbody>
            </table>
        </div>
        
        <div class="database-footer">
            SYSTEM STATUS: <span class="status-active">ONLINE</span> | 
            LAST UPDATE: <span id="current-date">[REDACTED]</span> | 
            ACCESS LEVEL: <span class="clearance-badge">MANAGERS CLEARANCE REQUIRED</span>
        </div>
    </div>
    
    <script>
        // Login System
        document.addEventListener('DOMContentLoaded', function() {
            // Credentials (Change these for production!)
            const validCredentials = {
                username: "SG-ADMINPANEL001",
                password: "REDLIGHTGREENLIGHT"
            };
            
            // DOM Elements
            const loginScreen = document.getElementById('login-screen');
            const databaseScreen = document.getElementById('database-screen');
            const loginButton = document.getElementById('login-button');
            const loginError = document.getElementById('login-error');
            const usernameInput = document.getElementById('login-username');
            const passwordInput = document.getElementById('login-password');
            
            // Update current date
            const updateDate = () => {
                const now = new Date();
                const options = { 
                    year: 'numeric', 
                    month: '2-digit', 
                    day: '2-digit',
                    hour: '2-digit',
                    minute: '2-digit',
                    hour12: false
                };
                document.getElementById('current-date').textContent = now.toLocaleString('en-US', options).replace(',', '');
            };
            
            // Login Function
            const attemptLogin = () => {
                const username = usernameInput.value.trim();
                const password = passwordInput.value.trim();
                
                if (username === validCredentials.username && password === validCredentials.password) {
                    // Successful login
                    loginScreen.style.opacity = '0';
                    
                    setTimeout(() => {
                        loginScreen.style.display = 'none';
                        databaseScreen.style.display = 'block';
                        document.title = "Squid Game: Legion [RP] Staff Only Database";
                        updateDate();
                        
                        // Update date every minute
                        setInterval(updateDate, 60000);
                    }, 500);
                } else {
                    // Failed login
                    loginError.style.display = 'block';
                    passwordInput.value = '';
                    
                    // Shake animation
                    loginScreen.style.animation = 'none';
                    void loginScreen.offsetWidth; // Trigger reflow
                    loginScreen.style.animation = 'shake 0.5s';
                }
            };
            
            // Event Listeners
            loginButton.addEventListener('click', attemptLogin);
            
            passwordInput.addEventListener('keypress', function(e) {
                if (e.key === 'Enter') {
                    attemptLogin();
                }
            });
            
            // Add shake animation to styles
            const style = document.createElement('style');
            style.textContent = `
                @keyframes shake {
                    0%, 100% { transform: translateX(0); }
                    10%, 30%, 50%, 70%, 90% { transform: translateX(-5px); }
                    20%, 40%, 60%, 80% { transform: translateX(5px); }
                }
            `;
            document.head.appendChild(style);
        });
    </script>
</body>
</html>
